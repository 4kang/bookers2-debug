<h2>Follow User</h2>
<!--exists?メソッドとは、指定した条件のレコードがデータベースに存在するかどうかを真偽値で返すメソッドです。-->
<% if @users.exists? %>
 <% @users.each do |user| %>
  <table>
    <thead>
      <tr>
        <th></th>
        <th colspan="2"></th>
        <th colspan="3"></th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><%= user.name %></td>
        <td>フォロワー数:<%= user.followers.count %></td>
      </tr>
    </tbody>
  </table>
 <% end %>
<% else %>
 <p>ユーザーはいません</p>
<% end %>